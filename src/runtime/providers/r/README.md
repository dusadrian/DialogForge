# R Runtime Provider

This folder will implement the language-neutral runtime provider contract for R.

R-specific workspace inspection, data frame operations, declared/value-label behavior, package checks, help lookup, and startup scripts belong here.
