# Base App Bootstrap

This folder assembles the product-independent base app from shared pieces.

It may contain startup registration, default routes/windows, product selection, and composition code.

It must not contain feature logic. Feature logic belongs in neighboring shared folders such as `dataset-editor`, `workspace`, `dialog-runtime`, or `runtime`.
