# Python Runtime Provider

This folder is reserved for a future Python runtime provider.

It should implement the same runtime provider contract without requiring the base app to know Python-specific details.
